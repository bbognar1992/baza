# ÉpítAI Database SSL Configuration
# Generated automatically for .crt certificate

# Database connection (update with your actual connection string)
password=Cyrtoz-ziqrad-kummo8 
user=postgres.idohoalwmovuewvcpqcn
host=aws-1-eu-north-1.pooler.supabase.com 
port=5432 
dbname=postgres

# SSL Certificate Configuration
SSL_CERT_PATH=.streamlit/prod-ca-2021.crt
# SSL Mode (choose one):
# - require: Require SSL, but don't verify certificate
# - verify-ca: Require SSL and verify certificate authority  
# - verify-full: Require SSL and verify certificate and hostname
SSL_MODE=verify-ca

# Optional: Enable SQL query logging
SQLALCHEMY_ECHO=False

# Combined DATABASE_URL
DATABASE_URL=postgresql://${user}:${password}@${host}:${port}/${dbname}
